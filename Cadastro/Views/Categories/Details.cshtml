@model Cadastro.ViewModels.CategoryViewModel

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhes</h1>

<div>
    <h4>Categorias</h4>
    <hr />
    <dl class="row">

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IdCategory)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdCategory)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<div>
    @Html.ActionLink("Editar", "Edit", new { id = Model.IdCategory }) |
    <a asp-action="Index">Voltar</a>
</div>